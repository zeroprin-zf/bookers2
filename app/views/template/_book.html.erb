<h1>New book</h1>
  <%= form_with model: book do |f| %>

  <h5>Title</h5>
  <%= f.text_field :title %>
  <h5>Opinion</h5>
  <%= f.text_area :body %>
    <button class="btn btn-primary" type="submit">
      Create Book
    </button>
<% end %>

<!--テンプレートを使ってルートエラーが出たときはコントローラで変数を定義してない可能性が高い-->